#include <bits/stdc++.h>
using namespace std;
using lint = long long;
#define task "tenfile"
#define FOR(i, a, b) for (lint i = (a), _b = (b); i <= _b; i++)
#define FORD(i, b, a) for (lint i = (b), _a = (a); i >= _a; i--)
#define REP(i, n) for (lint i = 0, _n = (n); i < _n; i++)
#define fi   first
#define se   second




void solve() {
    //
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    //
    if (fopen(task".in", "r")) {
        freopen(task".in", "r", stdin);
        freopen(task".out", "w", stdout);
    }

    int tc = 1;
    //cin >> tc;
    while (tc--) {
        solve();
    }
    return 0;
}
